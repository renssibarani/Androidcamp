<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:name=".base.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_livin"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_livin"
        android:supportsRtl="true"
        android:theme="@style/Theme.MandiriApps"
        tools:targetApi="31">
        <activity
            android:name=".presentation.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.ProfilActivity"/>
        <activity android:name=".presentation.RegisterActivity"/>
        <activity android:name=".presentation.HomeMainActivity"/>
        <activity android:name=".presentation.message.view.DetailTransactionActivity"/>
    </application>

</manifest>